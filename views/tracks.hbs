<header>
      <h2>Title</h2>
      <h2>Listen</h2>
</header>

<div class="container">

  {{#each tracks}}
  <div class="songs">
    <div class="title">
      <p>{{name}}</p>
    </div>

    <div class="player">
      <audio controls src="{{preview_url}}"></audio>
    </div>
  </div>
  {{/each}}

</div>